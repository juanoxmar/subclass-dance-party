var lineupDancers = function() {
  var styleSettings = {
    top: '40%'
  };
  // select all dancers
  $('.dancer').css(styleSettings);
  $('.napoleon').css(styleSettings);
  // apply styleSettings
};